process to create store

create store -> Provide store to the app -> Now to the Provider give the store -> Create slices(logical separation)--> Careful about export in slices, need to export reducer and then actions as well --> add the slice in the app store .reducer
